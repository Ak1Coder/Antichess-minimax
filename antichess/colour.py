"""Importing enum to have enum classes"""
from enum import Enum

class Colour(Enum):
    """Enum class containing 2 constants WHITE = 0 and BLACK = 1 like chess pieces colours"""
    WHITE = 0
    BLACK = 1
